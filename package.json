{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "artifacts/*",
    "scripts"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@workspace/kabadiwala-connect",
    "build": "npm run build --workspace=@workspace/kabadiwala-connect",
    "preview": "npm run preview --workspace=@workspace/kabadiwala-connect",
    "typecheck": "npm run typecheck --workspace=@workspace/kabadiwala-connect",
    "lint": "npm run typecheck"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "@types/leaflet": "^1.9.22",
    "leaflet": "^1.9.4"
  }
}
